services:
  rocky_linux8_snmp_container:
    build:
      context: .
    container_name: rocky_linux8_snmp_container
    volumes:
      - ../../:/ezsnmp
    working_dir: /ezsnmp
    entrypoint: ["/ezsnmp/docker/rockylinux8/DockerEntry.sh"]
