services:
  alma_linux10_snmp_container:
    build:
      context: .
    container_name: alma_linux10_snmp_container
    volumes:
      - ../../:/ezsnmp
    working_dir: /ezsnmp
    entrypoint: ["/ezsnmp/docker/almalinux10/DockerEntry.sh"]
