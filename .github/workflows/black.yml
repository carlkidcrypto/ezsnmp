name: Black

on:
  push:
    branches: [ dev/v2.0.0 ]

  pull_request:
    branches: [ dev/v2.0.0 ]

jobs:
  black:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: psf/black@24.10.0